public interface Payment {
    /* METODI */

    /*
     * Abstract. The method calculates and return returns the
     * salary as double (no functionality here).
     */
    abstract double calculatePayment();
    /* METODI END */
}
